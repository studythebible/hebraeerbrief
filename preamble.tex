\usepackage[scaled=0.92]{helvet}
\usepackage{booktabs}
\usepackage{amsthm}
\usepackage{lmodern}
\renewcommand*\familydefault{\sfdefault} %% Only if the base font of the document is to be sans serif
\usepackage[T1]{fontenc}



\makeatletter
% add Manu
\let\RequirePackage\original@RequirePackage
\let\usepackage\RequirePackage
%
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt
  \thm@postskip=\thm@preskip
}
\makeatother

% add Manu
\usepackage{color}
\usepackage{xcolor}
\usepackage{framed}
\setlength{\fboxsep}{.8em}
\usepackage[many]{tcolorbox}
%

% add Manu
\definecolor{aliceblue}{rgb}{0.94, 0.97, 1.0}

\newtcolorbox{blackbox_save}{
  colback=aliceblue,
  colframe=orange,
  coltext=black,
  boxsep=5pt,
  arc=4pt}

\newtcolorbox{blackbox}{}

\newenvironment{infobox}
  {
  \begin{itemize}
  \renewcommand{\labelitemi}{
    \raisebox{-.4\height}[0pt][0pt]{
      {\setkeys{Gin}{width=3em,keepaspectratio}
        \includegraphics{images/caution}}
    }
  }
  \setlength{\fboxsep}{1em}
  \begin{blackbox}
  \item
  }
  {
  \end{blackbox}
  \end{itemize}
  }

\newenvironment{definition}{
  \begin{itemize}
    \renewcommand{\labelitemi}{
      \raisebox{-.4\height}[0pt][0pt]{
        {\setkeys{Gin}{width=3em,keepaspectratio}
          \includegraphics{images/caution}
        }
      }
    }
    \setlength{\fboxsep}{1em}
    \begin{blackbox}
      \item
}{
  \end{blackbox}
  \end{itemize}
}

\newenvironment{biblebox}{
  \begin{itemize}
    \renewcommand{\labelitemi}{
      \raisebox{-.4\height}[0pt][0pt]{
        {\setkeys{Gin}{width=3em,keepaspectratio}
          \includegraphics{images/bible}
        }
      }
    }
    \setlength{\fboxsep}{1em}
    \begin{blackbox}
      \item
}{
  \end{blackbox}
  \end{itemize}
}

\newenvironment{quotebox}{
  \begin{itemize}
    \renewcommand{\labelitemi}{
      \raisebox{-.7\height}[0pt][0pt]{
        {\setkeys{Gin}{width=3em,keepaspectratio}
          \includegraphics{images/quote}
        }
      }
    }
    \setlength{\fboxsep}{1em}
    \begin{blackbox}
      \item
}{
  \end{blackbox}
  \end{itemize}
}
%
